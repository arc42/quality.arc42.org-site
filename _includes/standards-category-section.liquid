{% comment %}
Include for displaying a standards category section with minimal styling
Parameters:
- category: the category to filter by (e.g., "general", "safety", etc.)
- title: the display title for the section (e.g., "General", "Safety", etc.)
- standards: the collection of all standards
{% endcomment %}

## {{ include.title }}
{: .category-header}

{% assign filtered_standards = "" | split: "," %}
{% for standard in include.standards %}
  {% if standard.categories contains include.category %}
    {% assign filtered_standards = filtered_standards | push: standard %}
  {% endif %}
{% endfor %}

{% for standard in filtered_standards %}
    {% include standard-item.liquid standard=standard link=true %}
    <hr class="with-no-margin"/>
{% endfor %}

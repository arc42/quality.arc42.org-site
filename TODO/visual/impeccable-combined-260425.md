# Combined Impeccable TODO

Sources: Claude + Gemini violet critiques.

## P1

- [x] Make violet the structural brand color: define shared violet/cream tokens once, update `$brand-color`/`$brand-color-dark`, keep teal/blue as secondary or data accents only.
- [x] Remove old-brand residuals: reskin footer, page-title panels, tag boxes, synonym badges, blockquotes, and `also-known-as` styling to the violet/cream/neutral system.
- [ ] Rework the home graph section as an editorial figure or reference spread: remove the kicker, ghost CTA, and repeated note; use a quiet inline graph link or caption.
- [ ] Strengthen the home directory: show `01 / 02 / 03`, vary row copy, replace `<b>/<i>` count markup with spans, and tighten count text.
- [ ] Fix header/search usability: stronger search contrast, inline SVG search icon, skip link, `/` or `Cmd-K` search focus, and verified `aria-expanded`.
- [ ] Run the WCAG pass: contrast, focus states, scrollable graph region, placeholder contrast, and reduced-motion behavior.

## P2

- [ ] Replace the `Arial Black` brand tagline with a deliberate Libre Caslon or Atkinson treatment.
- [ ] Add a graph loading state so the home page never shows an empty bordered box.
- [ ] Define `Q42` on first use or use one product name consistently.
- [ ] Consolidate header details: nav tracking, collapse width, header height, border seam, search weight, and control radii.
- [x] Clean home markup/layout residue: unused H1 span, hard break vs. `text-wrap: balance`, hard-coded graph script/frontmatter mismatch, and `!important` graph-toggle override.

## P3

- [ ] Consider recent views and curated graph subset links for power users and trainers.
- [ ] Sweep sitewide for em dashes, side-stripe borders, and other banned visual patterns.
